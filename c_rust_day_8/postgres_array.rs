fn main() {

	/*
    * Rust's array's type and size are defined in a bracket separated
    * by a semicolon. Array values are stored in bracket instead of 
    * curly brackets.
    */
	let _binary: [i32; 2] = [1,0];
    
    println!("{} {}", _binary[0], _binary[1]);
    
}
